# Trip plan Lambda

API Gateway contract: `POST /trip-plan` with `origin`, `destination`, and optional ISO `currentTime`. The handler returns a demo route, departure recommendation, duration, and simulated congestion. Replace the local services with production integrations only when those integrations are configured and tested.
