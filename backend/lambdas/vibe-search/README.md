# Vibe search Lambda

API Gateway contract: `POST /vibe-search` with `vibeQuery` and optional `currentZone`. The current implementation classifies known demo keywords locally and returns simulated suggestions. Amazon Bedrock integration is a documented TODO, not an active dependency.
