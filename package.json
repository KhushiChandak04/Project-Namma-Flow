{
  "name": "namma-flow-workspace",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace frontend",
    "build": "npm run build --workspace frontend",
    "smoke": "npm run smoke --workspace frontend",
    "test:compass": "node scripts/testCompass.mjs",
    "install:frontend": "npm install --workspace frontend"
  }
}
