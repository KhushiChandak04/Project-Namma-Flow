export default function RouteResultCard({ result }) {
  // TODO: Janhavi — implement the final route result, departure, QR, and duration UI.
  // TODO: Titiksha — confirm the result fields supplied by Grid business logic.
  return result ? null : null
}
